framework:
    session:
        storage_factory_id: session.storage.factory.mock_file
    router:
        utf8: true

security:
    enable_authenticator_manager: true
    firewalls:
        main:
            lazy: true
